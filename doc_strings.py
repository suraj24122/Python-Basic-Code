def sum(a, b):
    '''This function takes two arguments a and b, adds them together, and returns the result.'''
    c = a + b
    return c;

print(sum.__doc__);
# Output: This function takes two arguments a and b, adds them together, and returns the result.
# The __doc__ attribute of the function contains the docstring.